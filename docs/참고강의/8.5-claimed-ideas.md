# 8.5 Claimed Ideas

## 커밋 요약

아이디어 클레임(Claim) 기능을 구현한 커밋입니다. 사용자가 아이디어를 클레임하면 소유권이 이전되고, 클레임된 아이디어는 다른 사용자가 볼 수 없도록 처리합니다. Mutations 분리, Action 함수 구현, Loader에서 조건부 리다이렉트, 조건부 렌더링 등의 패턴을 보여줍니다.

## 주요 변경사항

### 1. Mutations에 클레임 함수 추가

**구현**:
```typescript
// app/features/ideas/mutations.ts
export const claimIdea = async (
  client: SupabaseClient<Database>,
  { ideaId, userId }: { ideaId: string; userId: string }
) => {
  const { error } = await client
    .from("gpt_ideas")
    .update({ claimed_by: userId, claimed_at: new Date().toISOString() })
    .eq("gpt_idea_id", ideaId);

  if (error) {
    throw error;
  }
};
```

**패턴**:
- `update`를 사용하여 `claimed_by`와 `claimed_at` 필드 업데이트
- 에러는 그대로 throw하여 상위에서 처리
- 간단한 업데이트 작업

### 2. Queries에 클레임된 아이디어 조회 함수 추가

**구현**:
```typescript
// app/features/ideas/queries.ts
export const getClaimedIdeas = async (
  client: SupabaseClient<Database>,
  { userId }: { userId: string }
) => {
  const { data, error } = await client
    .from("gpt_ideas")
    .select("gpt_idea_id, claimed_at, idea")
    .eq("claimed_by", userId);

  if (error) {
    throw error;
  }

  return data;
};
```

**패턴**:
- 특정 사용자가 클레임한 아이디어만 조회
- 필요한 필드만 선택 (`select`)

### 3. Loader에서 조건부 리다이렉트

**구현**:
```typescript
// app/features/ideas/pages/idea-page.tsx
export const loader = async ({ request, params }: Route.LoaderArgs) => {
  const { client } = makeSSRClient(request);
  const idea = await getGptIdea(client, { ideaId: params.ideaId });
  
  if (idea.is_claimed) {
    throw redirect(`/ideas`); // 클레임된 아이디어는 상세 페이지 접근 불가
  }
  
  return { idea };
};
```

**패턴**:
- 데이터 조회 후 조건 확인
- 조건에 맞지 않으면 `redirect` throw
- 보안: 클레임된 아이디어는 다른 사용자가 볼 수 없도록 처리

**장점**:
- 서버 사이드에서 접근 제어
- URL 직접 접근 시에도 보호
- 사용자 경험 개선 (자동 리다이렉트)

### 4. Action 함수 구현 (클레임 처리)

**구현**:
```typescript
export const action = async ({ request, params }: Route.ActionArgs) => {
  const { client } = makeSSRClient(request);
  const userId = await getLoggedInUserId(client);
  
  const idea = await getGptIdea(client, { ideaId: params.ideaId });
  
  // 중복 클레임 방지
  if (idea.is_claimed) {
    return { ok: false };
  }
  
  await claimIdea(client, { ideaId: params.ideaId, userId });
  
  return redirect(`/my/dashboard/ideas`); // 클레임 후 대시보드로 이동
};
```

**패턴**:
- `getLoggedInUserId`로 인증 체크
- 중복 클레임 방지 (Race condition 대비)
- 성공 시 리다이렉트

**보안 고려사항**:
- 서버에서 다시 한 번 `is_claimed` 확인 (Race condition 방지)
- 인증된 사용자만 클레임 가능

### 5. 조건부 렌더링 (클레임 버튼)

**구현**:
```typescript
{loaderData.idea.is_claimed ? null : (
  <Form method="post">
    <Button size="lg">Claim idea</Button>
  </Form>
)}
```

**패턴**: 클레임된 아이디어는 클레임 버튼 숨김

### 6. IdeaCard 컴포넌트 개선

**주요 변경사항**:

#### 6.1 Props 타입 개선 (옵셔널 필드)

**변경 전**:
```typescript
interface IdeaCardProps {
  viewsCount: number;
  postedAt: string;
  likesCount: number;
}
```

**변경 후**:
```typescript
interface IdeaCardProps {
  viewsCount?: number;
  postedAt?: string;
  likesCount?: number;
  claimed?: boolean;
  owner?: boolean;
}
```

**장점**: 다양한 상황에서 재사용 가능 (대시보드, 목록 페이지 등)

#### 6.2 조건부 링크 비활성화

**구현**:
```typescript
<Link to={claimed || owner ? "" : `/ideas/${id}`}>
  <CardTitle className="text-xl">
    <span
      className={cn(
        claimed
          ? "bg-muted-foreground break-all selection:bg-muted-foreground text-muted-foreground"
          : ""
      )}
    >
      {title}
    </span>
  </CardTitle>
</Link>
```

**패턴**:
- 클레임되었거나 소유자인 경우 링크 비활성화 (`to=""`)
- 클레임된 아이디어는 시각적으로 구분 (회색 배경)

#### 6.3 조건부 메타데이터 표시

**구현**:
```typescript
{owner ? null : (
  <CardContent className="flex items-center text-sm">
    <div className="flex items-center gap-1">
      <EyeIcon className="w-4 h-4" />
      <span>{viewsCount}</span>
    </div>
    <DotIcon className="w-4 h-4" />
    {postedAt ? (
      <span>{DateTime.fromISO(postedAt).toRelative()}</span>
    ) : null}
  </CardContent>
)}
```

**패턴**: 소유자 대시보드에서는 메타데이터 숨김

#### 6.4 조건부 버튼 표시

**구현**:
```typescript
<CardFooter className="flex justify-end gap-2">
  {!claimed && !owner ? (
    <>
      <Button variant="outline">
        <HeartIcon className="w-4 h-4" />
        <span>{likesCount}</span>
      </Button>
      <Button asChild>
        <Link to={`/ideas/${id}`}>Claim idea now &rarr;</Link>
      </Button>
    </>
  ) : (
    <Button variant="outline" disabled className="cursor-not-allowed">
      <LockIcon className="size-4" />
    </Button>
  )}
</CardFooter>
```

**패턴**:
- 클레임되지 않고 소유자가 아닌 경우: 좋아요 버튼 + 클레임 버튼
- 클레임되었거나 소유자인 경우: 잠금 버튼 (비활성화)

### 7. Dashboard Ideas Page에 Loader 추가

**변경 전**:
```typescript
export default function DashboardIdeasPage() {
  return (
    <div className="space-y-5 h-full">
      <h1 className="text-2xl font-semibold mb-6">Claimed Ideas</h1>
      <div className="grid grid-cols-4 gap-6">
        {Array.from({ length: 5 }).map((_, index) => (
          <IdeaCard
            key={`ideaId-${index}`}
            id={index}
            title="..."
            viewsCount={123}
            owner={true}
            postedAt="12 hours ago"
            likesCount={12}
          />
        ))}
      </div>
    </div>
  );
}
```

**변경 후**:
```typescript
export const loader = async ({ request }: Route.LoaderArgs) => {
  const { client } = makeSSRClient(request);
  const userId = await getLoggedInUserId(client);
  const ideas = await getClaimedIdeas(client, { userId });
  return { ideas };
};

export default function DashboardIdeasPage({
  loaderData,
}: Route.ComponentProps) {
  return (
    <div className="space-y-5 h-full">
      <h1 className="text-2xl font-semibold mb-6">Claimed Ideas</h1>
      <div className="grid grid-cols-4 gap-6">
        {loaderData.ideas.map((idea) => (
          <IdeaCard
            key={idea.gpt_idea_id}
            id={idea.gpt_idea_id}
            title={idea.idea}
            owner={true}
          />
        ))}
      </div>
    </div>
  );
}
```

**패턴**:
- Loader에서 데이터 조회
- Mock 데이터 제거
- 실제 데이터 사용

### 8. SQL View 수정 (클레임된 아이디어 텍스트 숨김)

**변경**:
```sql
-- 변경 전
gpt_ideas.idea,

-- 변경 후
CASE 
  WHEN gpt_ideas.claimed_at IS NULL 
  THEN gpt_ideas.idea 
  ELSE 'ClaimedClaimedClaimedClaimedClaimedClaimedClaimedClaimedClaimedClaimedClaimedClaimed' 
END AS idea,
```

**패턴**: 클레임된 아이디어는 텍스트를 숨김 (보안)

**참고**: 실제 프로덕션에서는 더 나은 방법 고려 (예: NULL 반환)

## 적용 방안

### ✅ 이미 적용된 패턴

1. **Mutations 파일 분리**: `app/features/projects/mutations.ts` 생성 완료
2. **`getLoggedInUserId` 사용**: 여러 파일에서 이미 적용됨
3. **Action 함수 패턴**: 여러 페이지에서 사용 중
4. **조건부 렌더링**: 여러 컴포넌트에서 사용 중
5. **Loader에서 데이터 조회**: 여러 페이지에서 사용 중

### 🔄 추가로 적용 가능한 패턴

#### 1. Loader에서 조건부 리다이렉트 (우선순위: 높음)

**현재 상태**: 일부 페이지에서 사용 중이지만 일관성 부족

**적용 방안**:
```typescript
export const loader = async ({ request, params }: Route.LoaderArgs) => {
  const { client } = makeSSRClient(request);
  const resource = await getResource(client, { id: params.id });
  
  // 접근 권한 체크
  if (!resource || !hasAccess(resource)) {
    throw redirect("/unauthorized");
  }
  
  return { resource };
};
```

**적용 대상**:
- 프로젝트 상세 페이지 (소유자만 접근)
- 비공개 리소스 접근 제어
- 권한이 필요한 페이지

**장점**:
- 서버 사이드 접근 제어
- URL 직접 접근 시에도 보호
- 보안 강화

#### 2. Action에서 중복 처리 방지 (Race condition 대비) (우선순위: 높음)

**현재 상태**: 일부 Action에서만 적용

**적용 방안**:
```typescript
export const action = async ({ request, params }: Route.ActionArgs) => {
  const { client } = makeSSRClient(request);
  const userId = await getLoggedInUserId(client);
  
  // 현재 상태 확인
  const resource = await getResource(client, { id: params.id });
  
  // 중복 처리 방지
  if (resource.status === "completed" || resource.status === "processing") {
    return { ok: false, error: "이미 처리되었습니다." };
  }
  
  // 처리 로직
  await processResource(client, { id: params.id, userId });
  
  return redirect("/success");
};
```

**적용 대상**:
- 프로젝트 상태 변경
- 리소스 클레임/예약
- 중복 제출 방지가 필요한 Action

**장점**:
- Race condition 방지
- 데이터 무결성 보장
- 사용자 경험 개선

#### 3. 컴포넌트 Props 타입 개선 (옵셔널 필드) (우선순위: 중간)

**현재 상태**: 일부 컴포넌트에서 필수 필드로 정의되어 재사용 어려움

**적용 방안**:
```typescript
interface CardProps {
  id: number;
  title: string;
  viewsCount?: number;      // 옵셔널
  postedAt?: string;        // 옵셔널
  likesCount?: number;      // 옵셔널
  owner?: boolean;         // 옵셔널
  claimed?: boolean;       // 옵셔널
}
```

**적용 대상**:
- 재사용 가능한 카드 컴포넌트
- 다양한 컨텍스트에서 사용되는 컴포넌트

**장점**:
- 컴포넌트 재사용성 향상
- 유연한 Props 설계

#### 4. 조건부 링크 비활성화 (우선순위: 중간)

**현재 상태**: 일부 컴포넌트에서 사용 중

**적용 방안**:
```typescript
<Link to={disabled ? "" : `/resource/${id}`}>
  <CardTitle className={cn(disabled && "opacity-50 cursor-not-allowed")}>
    {title}
  </CardTitle>
</Link>
```

**적용 대상**:
- 비활성화된 리소스
- 권한이 없는 리소스
- 완료된 작업

**장점**:
- 시각적 피드백 제공
- 접근 제어 명확화

#### 5. SQL View에서 조건부 데이터 반환 (우선순위: 낮음)

**현재 상태**: View 사용 중이지만 조건부 처리 부족

**적용 방안**:
```sql
CREATE OR REPLACE VIEW resource_view AS
SELECT
  resource_id,
  CASE 
    WHEN is_private AND current_user_id != owner_id 
    THEN NULL  -- 또는 기본값
    ELSE content 
  END AS content,
  -- ...
```

**장점**: 데이터베이스 레벨에서 접근 제어

## 구현 우선순위

### 높음 (즉시 적용 권장)

1. **Loader에서 조건부 리다이렉트**
   - 영향도: 높음 (보안 강화)
   - 난이도: 낮음
   - 효과: 서버 사이드 접근 제어, 보안 강화

2. **Action에서 중복 처리 방지 (Race condition 대비)**
   - 영향도: 높음 (데이터 무결성)
   - 난이도: 낮음
   - 효과: Race condition 방지, 데이터 무결성 보장

### 중간 (점진적 적용)

3. **컴포넌트 Props 타입 개선 (옵셔널 필드)**
   - 영향도: 중간 (코드 품질)
   - 난이도: 낮음
   - 효과: 컴포넌트 재사용성 향상

4. **조건부 링크 비활성화**
   - 영향도: 중간 (UX 개선)
   - 난이도: 낮음
   - 효과: 사용자 경험 개선

### 낮음 (선택사항)

5. **SQL View에서 조건부 데이터 반환**
   - 영향도: 낮음 (보안 강화)
   - 난이도: 중간
   - 효과: 데이터베이스 레벨 접근 제어

## 참고사항

- Loader에서 리다이렉트는 `throw redirect()`를 사용해야 함 (return이 아님)
- Action에서 중복 처리 방지는 Race condition을 방지하기 위해 중요함
- 컴포넌트 Props는 재사용성을 고려하여 옵셔널 필드로 설계하는 것이 좋음
- 조건부 렌더링은 보안상 중요하지만, 서버 검증도 필수임

